{
  "name": "olympus_wallets",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ olympus_wallets",
    "create-local": "graph create --node http://localhost:8020/ olympus_wallets",
    "remove-local": "graph remove --node http://localhost:8020/ olympus_wallets",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 olympus_wallets"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.1",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
